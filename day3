Topics:
=======AND, OR, IN, BETWEEN, LIKE=========

  AND Operator : Used when all conditions must be true

    SELECT *
FROM employees
WHERE department = 'IT'
AND salary > 60000;

👉 Returns IT employees earning more than 60000.

  OR Operator: Used when at least one condition must be true.
SELECT *
FROM employees
WHERE department = 'HR'
OR department = 'Finance';
👉 Returns employees from HR or Finance.

  IN Operator: Used when matching multiple values in one column.
SELECT *
FROM employees
WHERE city IN ('Toronto', 'Vancouver');

    BETWEEN Operator: Used for ranges (inclusive).

    SELECT *
FROM employees
WHERE salary BETWEEN 45000 AND 70000;
👉 Returns employees earning between 45k and 70k.


      LIKE Operator (Pattern Matching): Used for searching patterns in text.

      for starts with we use
      SELECT *
FROM employees
WHERE name LIKE 'R%';

      for ends with we use
SELECT *
FROM employees
WHERE name LIKE '%a';

      for value contains
      WHERE name LIKE '%it%';

      for single character
      WHERE name LIKE '_a%';

      | Symbol | Meaning                  |
| ------ | ------------------------ |
| %      | Any number of characters |
| _      | Exactly one character    |



===========Practiced ========
  create database companydb;
use companydb;
CREATE TABLE employees (
    emp_id INT,
    name VARCHAR(50),
    department VARCHAR(50),
    salary INT,
    city VARCHAR(50)
);

INSERT INTO employees VALUES
(1, 'Rahul', 'IT', 60000, 'Toronto'),
(2, 'Anita', 'HR', 45000, 'Vancouver'),
(3, 'John', 'IT', 75000, 'Toronto'),
(4, 'Meera', 'Finance', 50000, 'Calgary'),
(5, 'David', 'HR', 40000, 'Vancouver');


-- Show employees from IT and salary > 60000.
select * from employees
where department='it' and salary>60000;
-- Show employees from HR or Finance.
select * from employees
where department in ('hr','finance');
-- Show employees in Toronto or Calgary.
select * from employees
where city in ('toronto','calgary');

-- Show employees with salary between 40000 and 60000.
select * from employees
where salary between  40000 and 60000 ;
-- Show employees whose name starts with 'A'.
select * from employees
where name like 'a%';
-- Show employees whose name ends with 'n'.
select * from employees
where name like '%n';

-- Show employees from IT in Toronto with salary between 50000 and 80000.
select * from employees
where department ='it' and city='toronto' and salary between 50000 and 80000;
-- Show employees not from Vancouver.
select * from employees
where city <> 'vancouver';
-- Show employees whose name contains letter 'a'.
select * from employees
where name like '%a%';
-- Show employees from HR or IT AND salary > 45000.
select * from employees
where department in ('hr','it') and salary >45000;
